<div class='container'>
  <%= form_for @additive, url: {action: "create"} do |f| %>
    <%= render 'shared/error_messages', object: f.object%>
    <%= f.text_field :name  %>
    <%= f.text_area :about %>
    <%= f.submit "Сохранить", class: "btn btn-large btn-primary" %>
  <% end %>
</div>
